<h2>Lista de categorias</h2>
<hr>
<a href="/admin/categorias/add"> <button class="btn btn-sucess">Nova categoria</button> </a>

{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>slug: {{slug}}</small><br>
            <small>data de criação: {{date}}</small>
        </div>
    </div>
{{else}}

{{/each}}